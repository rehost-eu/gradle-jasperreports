stages:
  - build
  - publish

build:
  stage: build
  image: gradle:jdk17
  script:
    - gradle build
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == 'master'

publish:
  stage: publish
  image: gradle:jdk17
  script:
    - gradle publish
  rules:
    - if: $CI_COMMIT_TAG